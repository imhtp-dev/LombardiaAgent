# Auto-activate conda environment when entering this directory
# Requires direnv (install: sudo apt-get install direnv)
# Setup: Add 'eval "$(direnv hook bash)"' to ~/.bashrc

eval "$(conda shell.bash hook)"
conda activate healthcare-agent
