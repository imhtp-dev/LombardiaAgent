# Base Requirements - Stable, large dependencies (rarely change)
# These are cached in Docker layers and only rebuild when this file changes

# Core Pipecat with all extras
pipecat-ai[daily,openai,silero,deepgram,elevenlabs]
pipecat-ai-flows>=0.0.20

# PyTorch (large downloads)
torch>=2.0.0
torchaudio>=2.0.0

# Core AI services
deepgram-sdk
openai
anthropic>=0.67.0
google-genai>=1.36.0

# Web framework
fastapi>=0.100.0
uvicorn>=0.36.0
websockets

# Core Python libraries
requests>=2.32.5
aiohttp>=3.8.0
pydantic>=2.0.0
numpy
uvloop>=0.17.0

# Utilities
python-dotenv~=1.0.1
loguru>=0.7.0
charset_normalizer>=3.4.3
urllib3>=2.5.0
python-json-logger>=3.3.0
rapidfuzz>=3.0.0
